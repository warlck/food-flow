apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: sales-system

spec:
  type: NodePort

  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
